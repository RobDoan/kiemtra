1.Define 1 constant giong define 1 bien thuong,nhung tat ca viet hoa
2.(a) (b) (c)
3. def test
		puts 'Dong 1'
		puts 'Dong 2'
		yeild 100
   end
   test {|i| puts "#{i}" }
4.  [1, 3, 8, 9, 19, 23, 42, 19].each do |i|
		if i % 2 != 0
			puts i
		end
	end
5.
6.$global_variable
7. a ||=b gia tri cua a se duoc thay bang gia tri cua b neu a la nill hoac false
8. 	-Public
	-Protected
	-Private
--------------Rails-------------
1.
2.  save

    valid

    before_validation

    validate

    after_validation

    before_save

    before_create

    create

    after_create

    after_save

    after_commit
 3.     _insert_callbacks,
        _normalize_callback_options
        after_action,
        append_after_action,
        append_around_action,
        append_before_action,
        around_action
        before_action
        prepend_after_action,
        prepend_around_action,
        prepend_before_action
        skip_action_callback,
        skip_after_action,
        skip_around_action,
        skip_before_action,
        skip_filter
#may em khong co rails
4.  rails new BookManagement
	rails g scaffold Book name:string, author:string, publish_on:date
5.	validates :name, presence: true
	validate :publish_in_future, :on => :create
	def publish_in_future
	    if publish_on && publish_on <= Date.today
	      errors.add(:publish_on, 'should be in the future')
	    end
  	end
6.rails g model Review content:string, user_id:integer
7.rails g model User name:string, email:string
9.  #book model
		has_many books
	#review model
		belongs_to book
		belongs_to user
	#user model
		has_many reviews

