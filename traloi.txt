1. De khai bao hang so :
    MY_CONSTANT = 1
2. b , c

3.
def print_data
    puts "Hello !"
    yield
    puts "Nice to meet you"
end

print_data {puts "My name is Jackie"}

4.
def get_odd(number_list)
    odd_list = []
    number_list.each do |n|
        if n % 2 != 0
            odd_list << x
        end
    end
    
    return odd_list
end
p get_odd([1, 3, 8, 9, 19, 23, 42, 19])

5.

6. $list_data = [1,2,4,8]

7. Neu a khong co gia tri thi se gan b vao a

8. Level : public, protected, private

========================================================

1.
http_verb    url               helper_method
GET          /photos           photos_path
GET          /photos/new       new_photos_path
POST         /photos           create_photos_path
GET          /photos/:id       show_photos_path
GET          /photos/:id/edit  edit_photos_path
PATCH/PUT    /photos/:id       update_photos_path
DELETE       /photos/destroy   delete_photos_path

2.
save
valid
before_save
after_save
validate
before_validation
after_validation
create
before_create
after_create
after_commit

3.
befor_action
after_action
before_filter
after_filter
skip_action_callback
skip_before_action
skip_after_action

    
